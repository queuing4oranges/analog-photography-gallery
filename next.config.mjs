/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
    domains: ['api2.queuing4oranges.com'],
  },
};

export default nextConfig;
